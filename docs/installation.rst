.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install headhunterru

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv headhunterru
    $ pip install headhunterru
